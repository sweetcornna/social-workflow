# TrendRadar 的关键词过滤表（上游 entrypoint.sh 校验它必须存在，内容可以为空）。
#
# 本项目**刻意留空**：选题筛选由 sourcing/selector.py 的 Claude Agent 按账号人设做，
# 在 sidecar 侧再过一道关键词只会让两处规则打架，而且改一次要重启容器。
# TrendRadar 只负责"把热榜抓下来存成文件"。
#
# 真要用的话，上游语法是：一行一个词组，空行分隔词组，
# `+词` 表示必须同时出现，`!词` 表示排除。例如：
#
#   人工智能
#   AI
#   +发布
#
#   裁员
#   !招聘
