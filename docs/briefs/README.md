# 阶段任务书（未公开）

`docs/briefs/` 原本存放 P10–P27 每一阶段发给实施子代理的任务书与验收报告（43 份）。
**没有随开源一起发布**：它们记的是本机绝对路径、worktree 名、其他私有仓库的引用，
以及一次性的过程材料——对读代码的人没有价值，对复现工程也不是必需。

每一阶段最终落地的结论都已经并进了代码、测试与 `docs/` 下的常设文档：
架构看 `docs/ARCHITECTURE.md`，工作台契约看 `docs/WORKBENCH_API.md`，
运维看 `docs/OPS.md` 与 `scripts/ops/README.md`，平台规则看 `docs/POLICY.md`。
